spring:
  application:
    name:  nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
      config:
        prefix: binbin
        server-addr: localhost:8848
        file-extension: yml